import SwiftUI

enum ListeningTheme {
    static let accent = AppColors.primaryBlue
    static let accentDark = AppColors.primaryBlueDark
    static let blobColor = AppColors.blobBlue

    static let listenFromTextAccent = Color(red: 0.20, green: 0.58, blue: 0.82)
    static let listenFromTextDark = Color(red: 0.12, green: 0.42, blue: 0.64)
    static let listenFromTextBlob = Color(red: 0.84, green: 0.92, blue: 0.98)

    static let importAudioAccent = Color(red: 0.55, green: 0.40, blue: 0.92)
    static let importAudioDark = Color(red: 0.38, green: 0.26, blue: 0.72)
    static let importAudioBlob = Color(red: 0.90, green: 0.86, blue: 0.98)

    static let importVideoAccent = Color(red: 0.16, green: 0.66, blue: 0.62)
    static let importVideoDark = Color(red: 0.08, green: 0.45, blue: 0.43)
    static let importVideoBlob = Color(red: 0.82, green: 0.95, blue: 0.93)

    static let savedPracticeAccent = Color(red: 0.93, green: 0.40, blue: 0.60)
    static let savedPracticeDark = Color(red: 0.62, green: 0.18, blue: 0.42)
    static let savedPracticeBlob = AppColors.blobPink
}
