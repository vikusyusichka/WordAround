import SwiftUI

/// Shadowing visual identity: purple accents over the shared app surfaces.
/// Mirrors the lightweight per-mode theme pattern used by Debate Mode.
enum ShadowingTheme {
    static let accent = Color(red: 0.54, green: 0.36, blue: 0.88)
    static let accentDark = Color(red: 0.36, green: 0.20, blue: 0.66)
    static let soft = Color(red: 0.90, green: 0.84, blue: 0.98)
}
