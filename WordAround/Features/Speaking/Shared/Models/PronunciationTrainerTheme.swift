import SwiftUI

/// Pronunciation Trainer visual identity: cyan / teal accents over the shared
/// app surfaces. Mirrors the lightweight per-mode theme pattern.
enum PronunciationTrainerTheme {
    static let accent = Color(red: 0.13, green: 0.66, blue: 0.74)
    static let accentDark = Color(red: 0.06, green: 0.42, blue: 0.50)
    static let soft = Color(red: 0.80, green: 0.94, blue: 0.96)
}
